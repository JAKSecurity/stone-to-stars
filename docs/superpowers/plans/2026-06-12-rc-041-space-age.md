# RC-041 Space Age Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development or superpowers:executing-plans.

**Goal:** Add the 9th `'space'` age per spec §B1 — `docs/superpowers/specs/2026-06-12-c4-space-age-last-stand-design.md` (the spec's tech/building/weapon tables are the data spec; follow them exactly).

**Architecture:** almost entirely additive data in the existing data-driven patterns. The `AgeId` union extension makes every full `Record<AgeId, …>` (e.g. `MAX_LEVEL_BY_AGE`, src/run/archetypes.ts:31) a compile error until a space entry exists — the sweep is compiler-driven first, then a grep audit for `Partial<Record<AgeId, …>>` maps and age-conditional logic the compiler can't see (hero map, audio/ambient age selection, biome/expedition age handling, civScreen age header capitalization handles any string).

### Task 1: Age + data (TDD)
- `src/game/types.ts`: `'space'` in `AgeId` + `AGE_ORDER`.
- Fix every resulting compile error with real entries: `MAX_LEVEL_BY_AGE.space` (continue the existing progression — one above modern's value), plus any other exhaustive Record.
- `src/tech/techData.ts`: rocketry / computers / satellites / planetary_defense per the spec table (costs, requires, gatesAge, unlocksBuilding, runBonus). NOTE: `planetary_defense` has NO runBonus and NO unlocksBuilding — its effect line comes in RC-042; that the tech card shows only cost is acceptable this ticket.
- `src/camp/buildingData.ts`: launch_pad, mission_control per spec (age 'space', maxLevel 3).
- `src/run/weaponData.ts`: `laser_array` — tier 'space', archetype from the existing set (bolt or piercer family per spec "beam/bolt"), budget-balanced vs modern weapons the way other age tiers step up (~15% over modern same-archetype numbers; `tests/weaponData-balance.test.ts` enforces the budget rule — make it pass legitimately, do not weaken the test).
- Tests: append space-age invariants to the existing age/tech test files (`tests/techAges.test.ts` pattern): 4 techs exist with exact requires/gates; buildings resolve; laser_array unlock path via satellites; AGE_ORDER length 9 with 'space' last. Existing tests asserting 8 ages get updated (intent preserved).

### Task 2: Art + age-keyed sweep
- `src/art/sprites/hero.ts`: `HERO_SPACE` (space-suit hero — white/visor variant in the established hero style); register in registry + `HERO_SPRITE_BY_AGE.space`.
- Building sprites `launch_pad`, `mission_control` in `src/art/sprites/buildings.ts` house style; projectile sprite for laser_array (thin cyan beam bolt) in `projectiles.ts` if `projectileSprite` references a new id.
- Grep sweep: `Partial<Record<AgeId`, `AgeId` switches, age string literals ('modern') used as "last age" sentinels; audio `startAmbient('civ', age)` recipe coverage for 'space' (reuse the modern recipe if the audio module needs an explicit mapping — follow its existing fallback semantics). List every site triaged in the report.
- Gates: `npx tsc --noEmit` + full `npx vitest run` + `npm run build`.

### Task 3 (controller): review, Playwright check at the C4-wide walkthrough, tracking.
